import "package:http/http.dart" as http;

class path_of_levels_API {
  /*Future<List<
  "Mymodel">> get_path_levels() async {
    try {
      dynamic response = await http.get(Uri.parse("URL_HERE"));
      print("response data from pathAPI");
         return guideModelFromJson(response);
    } catch (e) {
      print("error in path api is :"+e.toString());
      return [];
    }
  }*/
}
